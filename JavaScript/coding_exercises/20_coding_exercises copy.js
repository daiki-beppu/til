//  *if文の演習問題
// 条件文を使ってみましょう！
// この演習問題では、関数の中にコードを書いてもらいます（関数はまだ学んでないので、コメントで指している箇所にコードを書くことだけ守ってください）。

// 変数numが、コメントで囲われた場所で使えます
// 変数numが、もし偶数の場合はconsole.logで'even'と出力してください。（ヒント：偶数かどうかは剰余演算子%を使うことができます）
// 変数numが、偶数以外の場合は何もしないでください

function isEven(num) {
  //この中にコードを書いてください ↓↓↓↓
  if (num % 2 == 0) {
    console.log("even");
  }
  //この中にコードを書いてください ↑↑↑↑
}
